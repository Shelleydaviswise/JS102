$( document ).ready(function() {
  // Handler for .ready() called.

// Button event handler
$("#button").click(("#arraySource").val);

// To catch the array sent from the button
var $passedArray = $( "#arraySource" ).val();
  // var passedArray = $( "#arraySource" ).toArray() ;
  console.log(passedArray);




var newNumArray = [];
function over25() {
   for(var i = 0; i < passedArray.length; i++) {
     if (passedArray[i] > 25) {
      newNumArray.push;
    }
  }
}
   var sortedArray = []
    function arrayToSort(newNumArray){
    sortedArray = passedArray.sort;
    return sortedArray;
  console.log(sortedArray);
   }
});

fuck fuck fuck fuck !!!!